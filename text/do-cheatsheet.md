# The /do Cheat-Sheet: the whole lifecycle on one page

For a developer who knows `/do` exists but can't hold all twelve phases, their templates, skills, agents, and gates in their head. One page, no hunting across `000-do.md`, `do-new.md`, and `templates.md`.

**Benefit:** glance once, know exactly which template to copy, which skill runs, which agent fires, and which gate has to pass — for any phase.
