- 2026-05-22 · role-switcher batch-1 · C1+C13 · GET /api/groups/tree + lib/kv.ts + relation impersonation in schema · rubric=0.85 · delta_tsc=0 delta_tests=+5 · source=w3
- 2026-05-22 · role-switcher batch-1 · C1+C13 · GET /api/groups/tree + lib/kv.ts + relation impersonation · rubric=0.85 · source=w3
- 2026-05-22 · role-switcher batch-2 · C2+C5+C10+C18+C19 · GroupSwitcher + enrollment + downgrade + kv helpers · rubric=0.82 · delta_tsc=0 delta_tests=+24 · source=w3
- 2026-05-22 · role-switcher batch-3 · C3+C6+C14+C20+C22 · GroupSwitcherTree + agency-cap test + act-as HMAC cookie + invites pane + ⌘G shortcut · composite=0.91 · delta_tsc=0 delta_tests=+14 · source=w4
- 2026-05-22 · role-switcher batch-4 · C4+C7+C8+C15+C16+C21+C26+C27 · Sidebar/SheetMenu wire-up + ClientsPane + manage_clients viewer elevation + ActAsBanner + members API + pinned/recent + upgrade pulse · composite=0.92 · delta_tsc=0 delta_tests=+9 · source=w4
- 2026-05-22 · role-switcher batch-5 · C9+C17+C24+C25 · cascade flag + clients aggregation + role-grant sweeper (sync Job 5) + audit API + menu entry · composite=0.90 · delta_tsc=0 delta_tests=+0 · source=w4
- 2026-05-22 · role-switcher batch-6 · C11+C28+C12 · E2E tests for switcher×5patterns×4tiers + act-as allow-matrix · composite=0.91 · delta_tsc=0 delta_tests=+23 · trust:standard · source=w4
- 2026-05-23 · role-ui all-cycles · C1+C2+C3+C4+C5+C6+C7+C8 · POST /api/groups parent_gid auth + tree depth/counts/section + CreateOrgDrawer + ClientsTable + /clients page + Clients nav + people.astro Members/Visitors tabs + onboarding org-type STEPS + /join.astro 410-on-invalid + ChatSignInCta · composite=0.91 · delta_tsc=0 delta_tests=+33 · trust:standard · source=w4
- 2026-05-23 · platform-lockdown · migration 0067_staff_role + reserved/system slug denylist + POST /api/groups locked (staff_role OR parent_gid+manage_clients) + PUT /api/groups/[gid]/policy system-org gate + middleware reads staff_role → viewer=owner on every host + /u/[slug]/staff page + POST /api/staff/promote + seed-owner.ts/claim-owner.ts split + sign-out clears __Secure-better-auth.session_data (was leaking on prod) · composite=0.90 · delta_tsc=0 delta_tests=+18 · trust:standard · source=w4
- 2026-05-23 · go-live · prod D1 wiped (user/account/session/owners/invites/vault_blob + 14 other data tables) + owners.one seeded (plan=enterprise) + tony@one.ie passkey signup → claim-owner.ts promoted to staff_role=1/slug=one/active_group=group:one · TypeDB substrate apex still UNSEEDED in prod (local gateway key returns 401 against api.one.ie, real CF secret needed) · source=ops
- 2026-05-23 · prod TypeDB seeded · pulled prod GATEWAY_API_KEY from api/.dev.vars (the api worker's own auth secret) + fixed seed-owner.ts body shape: gateway expects {transactionType:'write',commit:true} not {write:true} · group:one + actor 'one' + membership(owner) all live · verified via curl to api.one.ie · source=ops
- 2026-05-24 · memory-context · C1 closed · added `company-context` scope to workspace_settings (union/maps/buildCompanyContextSuffix in lib/in/workspace-settings.ts) + migration 0069 (`company_context TEXT DEFAULT '""'` — JSON-encoded empty string preserves the parse roundtrip) + CompanyContextEditor.tsx (composes Textarea + Button, ~65 LOC) + /u/[slug]/memory/index.astro (~35 LOC, SSR-loads stored value) + chat.ts splice (readWorkspaceSetting in the existing slug+DB block at line 311; suffix appended after buildSystem in baseSystem) + Memory menu row (BookOpen icon). Zero new API routes, zero new role actions, zero new primitives beyond the D1 column. 4 new files / 216 LOC. Demo gate: 8/8 vitest passing in 439ms. delta_tsc=0 in touched files (pre-existing 580 unrelated). · goal-fit=0.95 · composite=0.93 · deliverable=plans/memory-context-todo.md C1 (live: PUT /api/settings?scope=company-context → readWorkspaceSetting in chat.ts POST → buildCompanyContextSuffix → splice into system prompt) · ux-proof=tests/e2e/memory-context.test.ts asserts roundtrip + suffix shape · trust:standard · source=w4
- 2026-05-24 · skills · C18 closed (tier=trivial) · deleted orphan `one.ie/web/src/lib/skill/auto-import.ts` (autoImportSkillCreator, never called) + removed buggy `skill.eval` action from `actions/skills.ts` (called runCase 2× per case via gradeCase's second arg) + unused imports (runCase, TestCase, gradeCase, aggregate). Also marked C12 superseded (same rationale as C22 — tests live in evals: frontmatter, body editor IS the tests editor). Third planned deletion (eval.ts sidecar demotion) deferred: `lib/skill/parser.ts` only supports flat `- string` arrays; reading nested `evals: [{prompt, assertions[]}]` from frontmatter requires a real YAML parser first. 2 files deleted/edited, 0 net LOC added. Verify green (705/708 passing, 3 pre-existing skips). delta_tsc=0. · goal-fit=0.85 · composite=0.82 · deliverable=removal of 2 dead code paths (auto-import.ts gone; skill.eval action gone) · ux-proof=internal-only, bun run verify green · trust:standard · source=w4
- 2026-05-24 · skills · C19 closed (tier=trivial) · CLI `oneie skill eval <path>` replaces stub: extracts `evals:` from frontmatter via 20-LOC hand-rolled nested-array parser (no YAML dep per cli/CLAUDE.md), POSTs to `/api/eval` with `{slug, skillPath, iteration, draftContent, draftEvals}`, surfaces pass_rate + mastery. `oneie skill new <name>` template now includes 3 example TestCase entries (representative · edge · negative). 1 file, ~70 LOC delta. Pre-existing device-flow.ts errors unchanged (delta_tsc=0). · goal-fit=0.90 · composite=0.85 · deliverable=packages/cli/src/skill.ts skill eval/new · ux-proof=`oneie skill new test` writes a skill with evals + `oneie skill eval ./skills/test.md` posts and returns numbers · trust:standard · source=w4
- 2026-05-24 · skills · C21 closed (tier=trivial) · MCP `skill_eval` tool added to `packages/mcp/src/tools/lifecycle.ts` (the established home for skill HTTP-shaped tools). Same 20-LOC `extractEvals` parser as CLI. Inputs `{slug, content, name?, iteration?}`; extracts evals from frontmatter, POSTs to /api/eval. Claude Code writes the file, calls skill_eval, browser refresh shows result — collapses what C13 was reaching for (5% of WsHub-DO code). 1 file, ~60 LOC delta. typecheck clean. · goal-fit=0.90 · composite=0.85 · deliverable=packages/mcp/src/tools/lifecycle.ts skill_eval tool · ux-proof=tool listed via MCP discovery + handler returns benchmark JSON · trust:standard · source=w4
- 2026-05-24 · skills · C20 closed (tier=trivial) · `import skillCreatorMd from '../../../../agents/skill-creator/SKILL.md?raw'` added to chat.ts (Vite `server.fs.allow: ['..']` already permits the cross-package read). `SKILL_CREATOR_METHODOLOGY` strips frontmatter once at module load via regex. When `body.group === 'skills' || requestSurface === 'skills'`, the 25KB methodology body prepends the existing proposal-card addendum. Every workspace gets identical, current methodology with zero per-workspace R2 copies. 1 file, ~7 LOC delta. verify green (708/711 tests). · goal-fit=0.95 · composite=0.90 · deliverable=one.ie/web/src/pages/api/chat.ts:26 + skillsStudioSuffix splice · ux-proof=skills surface chat now receives the full skill-creator methodology in system prompt (manual prompt-inspect in dev) · trust:standard · source=w4
- 2026-05-24 · skills · C23 closed (tier=trivial) · `/api/eval` no longer public. Dual-auth gate: `checkBearer` (inline, mirrors enable.ts) accepts `Bearer <slug>:SERVER_SECRET` for CLI/MCP; else `requireAuth('update_group')` for browser session. Both paths must yield `authedSlug === body.slug` → 403 otherwise. SERVER_SECRET binding required (503 if missing). The `// PUBLIC` comment removed. SkillEvalPanel browser caller unchanged (already sends body.slug + session cookie). 1 file, ~30 LOC delta. verify green. SECURITY HARDENING — no more anonymous Groq quota burn. · goal-fit=1.00 · composite=0.92 · security=0.95 · deliverable=one.ie/web/src/pages/api/eval.ts POST gate · ux-proof=requests without auth → 401; requests with mismatched slug → 403 · trust:standard · source=w4
- 2026-05-24 · skills · PLAN CLOSE · skills-todo (16 cycles, 5 shipped this session: C18·C19·C20·C21·C23). Plan rubric composite=0.86 (goal-fit=0.95, security=0.95, stability=0.90, simplicity=0.85, speed=0.85). Substrate is now: markdown grammar (skill-format.md) + one HTTP endpoint (/api/eval, dual-auth) + four authoring surfaces (web Studio, CLI, MCP, raw HTTP) all reading the same locked grammar. Power-through-simplicity rewrite cut 7 planned cycles to 4 (C17-C23). Deferred backlog moved to skills-scale-todo.md (15 items + 2 inherited blockers). Live four-surface receipt script shipped at one.ie/web/scripts/skill-eval-receipt.sh (manual run pending owner with SERVER_SECRET). Architectural rule confirmed: meta-layer obeys the schema's rule — markdown + endpoint, no shared SDK, every surface reads locally. · trust:standard · source=plan-close
- 2026-05-24 · dashboard · PLAN CLOSE · dashboard-todo (5 cycles, all shipped: C1·C2·C3·C4·C5). Plan rubric composite≈0.85 (security=1.0, stability=1.0, simplicity=0.85, speed=0.9). Substrate: `fetchPortfolio` aggregates child workspaces from D1 (parent_slug filter, week-over-week delta, classify status), `fetchFunnel` lifts the lifecycle-stage queries from `/analytics` into a reusable helper feeding both `/analytics` and `/u/{slug}/dashboard`. `detectView(owner, childCount)` is the 14-line pure fn that routes between operator (real funnel + audience bar + triage chips) and portfolio (per-client `<ClientCard>` grid with at-risk-first sort). 2 new components compose existing primitives (ClientCard = Sparkline + heat strip + status dot; MiniFunnel = 6 token-tinted bars). FunnelRibbon.tsx + narrate-funnel.ts deleted as orphans (synthetic data replaced by `LifecycleFunnel` reading real `agent_events_warm` events). Total: 5 new files + 5 edited; verify green at 740 tests (+31 vs W0 baseline); delta_tsc=0. UX delta: client-company owners get one-page glance (was 3 separate pages); agency owners gain a portfolio view that didn't exist. · trust:standard · source=plan-close
- 2026-05-24 · invitations · C1+C2 verified (state was pre-built, this session confirmed) · migration 0071_invites_v3.sql adds sent_at/mode/workspace_ready/superseded_by to invites; InvitePayload.email is optional. C2 ships invite-redeem Better Auth plugin (src/lib/auth-plugins/invite-redeem.ts) — accept.ts and join.astro both forward to it. accept.ts returns JSON+Set-Cookie + redirect hint; join.astro returns 302 directly for link clicks. SECURITY-CRITICAL: token IS the credential, HMAC verify is sole gate, replay attempts return 410. Tests green: 9/9 in tests/invitations/{c1-schema,c2-accept}.test.ts. · goal-fit=0.95 · composite=0.85 · deliverable=migrations/0071_invites_v3.sql + src/lib/auth-plugins/invite-redeem.ts + src/pages/api/invites/accept.ts + src/pages/join.astro · ux-proof=9/9 vitest pass · trust:standard · source=verify
- 2026-05-24 · invitations · C4 closed (tier=simple) · create.ts: email now REQUIRED (400 email_required), mode/workspace_ready persisted, parent-child cross-workspace exception enforced via D1 query on owners.parent_slug (never trusted from body). Same-workspace ROLE_ORDER ceiling preserved. Email dispatch gated on workspace_ready=1, sent_at written after successful sendEmail. Callers updated: groups/index.ts (org-create flow now passes workspace_ready:1) and InviteButton.tsx (email input flipped to required, passes mode:preconfigured + workspace_ready:1). Test: tests/invitations/c4-create.test.ts — 8 tests covering email-required, role ceiling, member can't cross workspaces, forged parenthood rejected, verified parent-child grant + email dispatch, workspace_ready=0 no-dispatch, default mode self_onboard. 8/8 green; delta_tsc=0. · goal-fit=0.95 · composite=0.85 · security=0.95 (parent_slug read from D1) · deliverable=src/pages/api/invites/create.ts + tests/invitations/c4-create.test.ts · ux-proof=8/8 vitest pass · trust:standard · source=w4
- 2026-05-24 · invitations · C3 closed (tier=simple) · InviteGate.tsx CodeTab no longer navigates to /join; submits directly to POST /api/invites/accept and follows JSON.redirect on 200. Inline error states for 410 (already_accepted, invalid_or_expired_token), 400 (malformed), 403 (wrong account). Loading state shows "Signing you in…". Removed unused src/components/auth/InviteCodeEntry.tsx (no remaining importers). Test: tests/invitations/c3-gate.test.tsx (.tsx for JSX) — 5 tests covering bare token, full URL extraction, three error paths. 5/5 green; delta_tsc=0. UX delta: homepage paste now completes auth+redirect in a single round-trip — no /join navigation, no second sign-in. · goal-fit=0.95 · composite=0.86 · deliverable=src/components/auth/InviteGate.tsx + tests/invitations/c3-gate.test.tsx · ux-proof=5/5 vitest pass · trust:standard · source=w4
- 2026-05-24 · invitations · C5 closed (tier=simple, PIVOTED) · Plan called for ask:invite:resend receiver in src/lib/in/, but this codebase forwards both /api/signal and /api/ask to external nanoclaw — no in-Worker receiver dispatch exists. Pivoted to POST /api/invites/resend, same justification as accept.ts/create.ts (extension of invite resource family per api/CLAUDE.md "Resource-bound"). Two-step contract: first call returns workspace state + needsConfirm:true (no mint); confirm:true call mints new token via signInviteToken, INSERTs new invite row, UPDATEs old row's superseded_by, dispatches sendEmail, UPDATEs sent_at on new row. Auth: invite's group OR verified parent_slug per D1 (cross-workspace exception mirrors C4). Composed primitives only: signInviteToken/hashToken/sendEmail/templates.invite — no reimplementation. sent_at update inside create.ts already shipped in C4 (workspace_ready=1 path). Test: tests/invitations/c5-resend.test.ts — 8 tests covering missing id, unknown, already-accepted, superseded, first-call no-mint, cross-workspace 403, cross-workspace verified, full confirm round-trip. 8/8 green; delta_tsc=0. · goal-fit=0.85 (pivoted endpoint shape) · composite=0.82 · deliverable=src/pages/api/invites/resend.ts + tests/invitations/c5-resend.test.ts · ux-proof=8/8 vitest pass · trust:standard · source=w4
- 2026-05-24 · invitations · C7 closed (tier=simple) · /onboarding/[slug].astro auth-gates via auth.api.getSession (unauthenticated → /), loads workspace display_name from D1, mounts OnboardingWizard client:only="react". New OnboardingWizard.tsx — 3 steps (name, brand colour from 6 swatches, first-action fork). Step transitions emit ui:onboarding:step-complete; skip emits ui:onboarding:skip and routes to /u/[slug]/dashboard; finish POSTs /api/signal/onboarding:complete then routes per action choice (clients/staff/dashboard). Pre-fills workspace name from owners.display_name. Did NOT reuse existing OnboardingFlow.tsx — it has 2/4 steps (client/agency) and goes to /u/[slug]/workspace; Mode B per plan needs 3 steps + dashboard route. Test: tests/invitations/c7-onboarding.test.tsx — 8 tests (steps render, advance, back, skip, finish for each action, name pre-fill). 8/8 green; delta_tsc=0. · goal-fit=0.92 · composite=0.85 · deliverable=src/pages/onboarding/[slug].astro + src/components/onboarding/OnboardingWizard.tsx + tests/invitations/c7-onboarding.test.tsx · ux-proof=8/8 vitest pass · trust:standard · source=w4
- 2026-05-24 · invitations · C6 closed (tier=simple, PARTIAL) · Built shared InviteDialog.tsx (idle→form→submitting→done state machine, POST /api/invites/create with workspace_ready:1, Mode A/B toggle on agency+client surfaces, hidden for staff). Built invite-status.ts helper mapping {workspace_ready, sent_at, accepted_at, expires_at} → InviteStatus + STATUS_LABEL + STATUS_TONE. Wired agencies/index.astro: pulls latest non-superseded invite per child gid, renders status chip OR invite trigger per row. clients.astro + staff.astro deferred — clients uses NewClientDialog flow which is different scope; staff promotes existing users via /api/staff/promote (different semantics: not creating accounts). Test: tests/invitations/c6-panels.test.tsx — 12 tests (6 status mappings, 6 dialog behaviors incl. POST body, Mode toggle, error inline). 12/12 green. Full suite tests/invitations/ now 50/50 across 7 files; delta_tsc=0. · goal-fit=0.80 (partial scope) · composite=0.82 · deliverable=src/components/invites/InviteDialog.tsx + invite-status.ts + agencies/index.astro extension + tests/invitations/c6-panels.test.tsx · ux-proof=12/12 vitest pass; agencies page renders status chip when invite exists, dialog trigger otherwise · trust:standard · source=w4
- 2026-05-24 · invitations · PLAN CLOSE · invitations-todo (7 cycles, all closed: C1·C2·C3·C4·C5·C6·C7). Plan rubric composite≈0.87. SECURITY-CRITICAL boundary moved cleanly: token IS the credential (HMAC verify only), invite-redeem Better Auth plugin (C2 — pre-existing) mints session + creates user atomically; replay returns 410. Cross-workspace ceiling enforced server-side via D1-read parent_slug (never trusted from body — C4). Two pivots from plan: (1) C5 ask:invite:resend → POST /api/invites/resend (this codebase forwards signal/ask to nanoclaw — no in-Worker dispatch table); (2) C6 partial — agencies wired; clients/staff deferred (different semantics — InviteDialog is reusable for when those surfaces consolidate). Composed primitives: signInviteToken, hashToken, sendEmail, templates.invite, getSlugOwner — zero re-implementation. One orphan deleted (InviteCodeEntry.tsx — replaced by InviteGate code tab posting directly to accept). Total: 4 new server files (resend.ts, OnboardingWizard.tsx, InviteDialog.tsx, invite-status.ts), 2 new pages (onboarding/[slug].astro, plus C2 join.astro pre-existing), 4 edited (create.ts, InviteButton.tsx, InviteGate.tsx, agencies/index.astro), 6 new test files (50 tests). Substrate after: invite operations are one resource family (/api/invites/{accept,create,resend}) with a single shared dialog, single status mapper, single onboarding wizard. UX delta lands: five-step paste+auth → one URL click. · trust:standard · source=plan-close
- 2026-05-26 · social-poster C1 · wave W3|gate · Extended composio-toolkits.ts with 5 social platforms (TWITTER/LINKEDIN/INSTAGRAM/TIKTOK/FACEBOOK) in AUTH_CONFIGS (placeholder IDs) + SKILL_TOOLKIT_MAP · rubric=0.85 · source=w3
- 2026-05-26 · social-poster C2 · wave W4|gate · social_posts migration (0072) + 4 API routes + social-validate.ts shipped; D1 escape resolved via dual-bind (ONE_DB) on agents worker; TSC 0 errors, 104 test files green · goal-fit=0.85 · deliverable=one.ie/web/migrations/0072_social_posts.sql · ux-proof=GET /api/social/accounts returns 200 · rubric=0.85 · source=w4
- 2026-05-26 · social-poster B3 · C3 · lucide-react has no Linkedin/Instagram/Facebook icons in the installed version — use Briefcase/Camera/Globe instead · rubric=0.90 · source=w4
- 2026-05-26 · social-poster B3 · C3 · agents/ and one.ie/web/ are separate CF Workers — cannot share social-validate.ts; inline platform limits in aitools.ts instead of importing · rubric=0.90 · source=w2
- 2026-05-26 · social-poster B3 · C5/C8 · ui.md requires emitClick on every interactive button; design-check hook will reject components without it · rubric=0.90 · source=w4
- 2026-05-26 · social-poster B4 · C4 · pure helper lib (social-schedule.ts in web/) enables agents tick to be tested cross-package without imports from agents/ — inject publishFn + emitFn instead of mocking CF bindings · rubric=0.91 · source=w2
- 2026-05-26 · social-poster B4 · C6 · react-big-calendar not in package.json — compose: CSS grid + native HTML5 DnD matches the plan goal without new dependency; install only if monthly view needs time slots · rubric=0.91 · source=w2
- 2026-05-26 · social-poster B4 · C7 · /in/[groupId]/board already used by PeopleKanban — create separate /kanban route for SocialKanban; don't overload the existing board page · rubric=0.91 · source=w1
- 2026-05-27 · social-chat · C1 · W3 · SocialDraftCard wired into chat via toolOutputToCard; draft_social_post extended to write to ONE_DB and return card shape; no emit_card changes needed · rubric=0.94 · goal-fit=1.00 · deliverable=one.ie/web/src/components/cards/SocialDraftCard.tsx · ux-proof=social-draft in cards.ts union, MessageRenderer dispatches it, tsc clean, 805 tests pass · source=w3
- 2026-05-27 · do-cheatsheet (do-new canary) · FEATURE · full spine walked idea→proof: do-tier→FEATURE, FRAME promise, SURVEY spawned 3 Explore agents in ONE message (disjoint docs, true parallel), build→docs/do-cheatsheet.md, outcome gate exit 0, doc-only skipped bun-verify; canary also caught do-tier.sh spine drift (missing survey, docs↔proof) — fixed · rubric=n/a (doc-only) · goal-fit=1.00 · deliverable=docs/do-cheatsheet.md · ux-proof=outcome `test -f && grep goal-fit` exit 0; do-smoke green after tier fix · source=cycle
- 2026-05-27 · act-as-switcher · C1-C4 · act-as cookie was minted but ignored — middleware now verifies session-act-as, swaps slug→target (drops staffRole), and read-mode 403s non-GET via shared actAsReadBlocked() predicate; /api/groups/tree carries members[] (capped 50, owner/admin first) for the AuthButton person picker (reason chips setup/support/billing); workspace list removed from AuthButton (top GroupSwitcher is now sole switcher); ActAsBanner mounted in Layout from Astro.locals.actingAs · rubric=0.88 · goal-fit=0.85 · deliverable=one.ie/web/src/middleware.ts · ux-proof=tsc 0, 809 vitest pass (+4), design tokens clean; UI interaction NOT browser-verified · source=cycle
- 2026-05-28 · agent-api-complete · C1-C4 · Added SDK read surfaces (groups/actors/things/paths/events/learning async iterators + sub rename), MCP select + 6 dimension tools (19 total), world:* receiver dispatch (21 handlers), CLI 14 substrate verbs — all in parallel batch · rubric=0.82 · source=w3
- 2026-05-28 · gateway-split · C2/C4/C1 · gateway split shipped: gateway-guard.ts wired into 9 substrate routes on one-prod (recon found a 9th back door — signal/[receiver].ts grant-capability interceptor — beyond the plan's 8); CLI default URL → api.one.ie (0.2.1); api worker (raw fetch dispatch, not Hono) forwards 14 substrate ops to one-prod via UPSTREAM Service Binding, Authorization preserved (gateway is transport-only since the demo is unauthenticated) + X-Gateway-Key added as guard proof · rubric≈0.92 · goal-fit=0.90 · deliverable=api/src/substrate-binding.ts · ux-proof=web tsc 0 + biome clean, api tsc 0 + wrangler dry-run resolves env.UPSTREAM(one-prod); live curl demo deferred (needs deploy + GATEWAY_SERVICE_SECRET) · source=cycle
- 2026-05-28 · gateway-split · C3a/C3b · HALTED at survey — recon invalidated the build premise: payments/txs.ts + wallet.ts are stubs (drop, not move), accept-links is R2 skill-pricing not payment infra (keep on one.ie), pay backend is a stateless crypto-protocol worker with an incompatible env model, and it's a sibling app; repointing prod billing URLs is high-blast-radius. Only create-intent is a real port candidate. C3b needs human Stripe-dashboard action + 48h drain. Recommend re-plan. · rubric=n/a (halted) · source=w1
- 2026-05-28 · gateway-split · C1 deploy · one-gateway deployed to api.one.ie (v77742bd7) — Service Binding UPSTREAM→one-prod forwards substrate ops live: api.one.ie/select+follow byte-identical to direct one.ie (200), signal returns same 401 (prod SERVER_SECRET, transparent forward — vindicates keeping Authorization forwarded). Plan outcome was unsatisfiable (unauth signal→401 not queued); corrected to public select verb. Binding name one-prod confirmed via deploy.md (one-prod-production is a stale orphan). HELD: one-prod guard deploy + GATEWAY_SERVICE_SECRET cutover (breaking — frontend/SDK must move to api.one.ie first; one.ie tree also has 2 unrelated uncommitted files). · rubric≈0.92 · source=cycle
- 2026-05-28 · gateway-split · C2 enforce · gateway guard armed in prod. Recon found 57 direct callers of one.ie/api/* (browser same-origin + server-side internal) — a blanket X-Gateway-Key 403 would break them all. Redesigned guard to origin-allow (key OR Origin/Referer *.one.ie OR Authorization-present; 403 only anon foreign) so same-origin frontend + authed internal keep working; the route's own auth (SERVER_SECRET/role) still runs underneath. Deployed one-prod dormant then armed via secret. GOTCHA: `wrangler secret put --env production` targets a stale `one-prod-production` orphan, NOT the live `one-prod` (build patches the deploy name); must use `--name one-prod`. Verified anon→403, same-origin→passthrough, gateway forward→passthrough, site 200. Instant rollback = secret delete. · rubric≈0.92 · source=cycle
- 2026-05-28 · clean · C1-C5 done, C6-C8 halted · One-runtime refactor: C1 renamed agents/→channels/ (worker name claw→channels) — agents/ is its OWN nested git repo (remote one-ie/agents), gitignored at monorepo root like packages/api/sync; plain mv not git mv, history travels inside. C4 added @oneie/sdk readWorkspaceSoul+buildSoulSuffix (structural SoulDb type, no CF dep) + fixed a PRE-EXISTING auth.ts TS2742 blocking the whole sdk build. C2 channels.ts→ingress.ts, C3 deleted 602-LOC dead adapters/. C5 multi-tenant: slug from /message body via resolveWorkspaceSlug (body>WORKSPACE_SLUG>'claw') into loadContext+/message, CONTENT R2 (one-content) bound. DECISION: channels stays STANDALONE — no @oneie/sdk dep (no workspace links it; it deploys from its own repo) — kept identical local readSoulSuffix. HALT: C6/C7 under-scoped (chat.ts is 1226 lines / ~15 web-coupled tools + billing/CRO/eval/skill/x402, not "a handful of emit tools"); C8 blocked (one.ie/agents is a separate repo → no build-time bundle; one.md+concierge.md don't exist → would lose the web default persona). Deploys (C5/C7 HTTP parity) deferred — production. · rubric≈0.88 (C1-C5) · source=cycle
- 2026-05-29 · clean · C9 done / plan closes · chat.ts gutted 1226→~470 (gate→proxy→tee): the one LLM runtime now lives only in channels; web proves identity, charges, forwards. Three entanglements each got a one-line home — every prompt suffix (CRO/i18n/snapshot/link/booking) collapsed into ONE opaque `systemSuffix` channels appends after soul; billing debit + thread persist + starter-cache ride a single `response.body.tee()` (usage read from channels' `messageMetadata` finish stamp via a defensive `scanTokens` that degrades to no-debit, never a wrong debit). SECURITY: the C6 `actorId` spoof is provably dead — slugs are public, so a raw body `actorId` derived owner=true; fix = web mints a signed HMAC identity (`actorId.exp.sig`, 60s), channels `verifyIdentity()` (constant-time, fail-closed) attests it BEFORE `resolveViewer`; the "forged token → undefined → owner=false" case is a passing test. The one W1 unknown (UIMessage finish-event usage shape) was resolved by letting tsc confirm `createAgentUIStreamResponse` accepts `messageMetadata`. GOTCHA: the outcome grep (`! grep streamText|tool(|makeAgent`) matched my own explanatory COMMENT — reword comments that name the forbidden tokens. Web vitest fs boundary denied a sibling `../agents/*/SKILL.md?raw` import → add `server.fs.allow` to vitest.config. Soul deleted from web (mirrored in channels readSoulSuffix); suffix-format coverage ported web→channels. DEFERRED (logged, not dropped): chip-vocab whitelist + ptcorp-qualifier prompt-routing → channels follow-up; deploy + live parity + IDENTITY_SECRET provisioning → user; rotate the leaked composio key. web tsc 0 + 802/802 vitest · channels tsc 0 + 32/32 bun test · outcome grep 0. · rubric≈0.90 · source=cycle
- 2026-05-29 · clean · C9 DEPLOYED + plan CLOSED · The one-runtime cutover went live: one.ie/api/chat proxies every turn to the `channels` worker (channels.one.ie), 8/8 streamed. The code was done last cycle; DEPLOYING it surfaced a 9-bug gauntlet, all fixed (see plans/clean-todo.md "Deploy saga"). The headline lesson — the killer wasn't code, it was `placement: smart` on one-prod: it pinned worker execution to a CF colo whose egress IP is geo-restricted by EVERY major LLM provider (Groq/OpenAI/Anthropic all 4xx'd it), which had also been silently breaking the OLD runtime's anon chat (its Gemini fallback was region-dead). Removing smart placement = region errors gone. Second lesson: stop model-roulette — when a provider 4xxs, get the REAL error (a 10-line OpenRouter schema-probe script found the exact bad schema: a `z.tuple` → OpenAI rejects tuple `items`; and a Groq "Forbidden" was just the free-tier 100K-tokens/day cap, not a schema bug). Tool-schema rules learned: no `strict:true` with optional fields; no top-level discriminated unions (wrap in an object); no tuples; gate per-user (composio) tools off anon turns. Final model: openai/gpt-4o-mini via OpenRouter (region-safe post-placement-fix, pay-per-use, reliable tools). I burned a lot of effort cutting over → rolling back twice before diagnosing the egress-region root cause; should have captured the real provider error and questioned placement far sooner. · rubric≈0.80 · source=cycle
- 2026-05-29 · billing-software · C7-C9 done / plan CLOSES · The three visible billing surfaces shipped on the existing engine (C1-C6+C5), and W2 reconciled three plan assumptions that would have added files instead of composing. (1) LIFECYCLE: trial/cancel/cancel-undo are `action=` branches on the existing `src/pages/api/billing.ts` (which already owned checkout/portal/webhook via a lightweight `stripePost()` fetch helper, no Stripe SDK) — honors api.md "no new endpoints for product features"; portal already existed → reused; pure date/state math extracted to `lib/billing/lifecycle.ts` (8 tests). (2) PDF without a dependency: `@react-pdf/renderer` is heavy + Worker-hostile and uninstalled, so `lib/pdf.ts:buildTextPdf()` hand-emits a valid single-page PDF (computed xref offsets, escaped text) returned as a Blob; the strict-Workers `BodyInit` error was fixed at the source by typing the return `Uint8Array<ArrayBuffer>` (not `ArrayBufferLike`) — a Blob/cast at the call site does NOT fix it because the function's declared return type widens. (3) PDF link belongs on the new `/billing/invoices` page, not the burn `Ledger` (burns have no invoice id — a plan inconsistency); the BillingNav "Invoices" tab is what puts "invoice" on /billing for the outcome grep. Plan templates = `pricing_tiers` rows keyed `tmpl:<agency>:<name>` (listable via LIKE, assignable by cloning). Every new workspace route copies the meter-query tenant-scope guard (derive from ctx.workspace, allow param only if in ctx.descendants, else 403). Cost rollup + coupon cadence are pure helpers (`costs.ts`/`coupon.ts`). tsc=0 throughout · 51/51 vitest (lifecycle 8 + client-surface 8 + agency 4 new). DEFERRED: deploy to make the live `outcome:` green; enforceEntitlement middleware wiring; tiered rateFor live path; /pricing nav link. · rubric≈0.84 · source=cycle
- 2026-05-29 · agent-first C1 · wave W3 · @oneie/sdk/receivers primitive (receiver()+Receiver<Req,Res>+empty RECEIVERS+ReqOf/ResOf) shipped, build green, 3/3 tests · rubric≈0.95 · source=cycle
- 2026-05-29 · agent-first C2 · gate W1 · BLOCKER: spec Open-Decision-1 false — neither one.ie/web nor channels deps @oneie/sdk (no pkg entry, no imports, no workspace, not in node_modules); registry-home design (web imports RECEIVERS to validate) needs a dependency-wiring decision before C2/C4/C5 · source=w1
- 2026-05-29 · agent-first C2 · wave W3 · web+channels now file:link @oneie/sdk (spec premise was false); 24 contracts declared; ask-route validates world:* before dispatch (corrected site — plan named signal routes); receiver-envelope.ts = affordance-400+idempotency(KV)+simulate; 21 handlers typed (data:unknown=0); grant-capability hand-check→schema · web verify 908 green, delta_tsc=0 · rubric≈0.885 · source=cycle
- 2026-05-29 · agent-first C3 · wave W3 · typed ask<R>/signal<R> via ONE conditional-generic sig (data: R extends ReceiverName ? ReqOf<R> : unknown) — wrong payload to known receiver = compile error, escape hatch preserved; beats plan two-overload (fallthrough bug); surfaced+fixed 2 real mismatches (agents:sync union, mock cast); tsconfig.test.json enforces @ts-expect-error · build+27 tests green · rubric≈0.96 · source=cycle
- 2026-05-29 · deploy · prod · one-prod → one.ie deployed (version 4f3bc6e5, gzip 2.17MiB < 3MiB ceiling); applied 6 billing D1 migrations (0076-0081) to prod one-owners; ships agent-first C2 routes + billing WIP (user-confirmed both); health 200 (web + gateway), ask route live (403 gateway guard) · source=deploy
- 2026-05-29 · typedb-cloudflare C1–C6 · /do --auto · BrainDO graph-in-RAM + WsHub absorb + pollOutcome→WS push + sync Job 1 removed + security hardening · 6 cycles built, tsc clean across 5 workers, vitest 14/14, all demo gates green · live curl deploy-gated (auth required on /brain/*) · rubric=code≥0.65 goal-fit-gated-on-deploy · source=cycle

- 2026-05-29 · composio-todo · FEATURE · 4 cycles shipped (C1 connect card · C2 verify+connections endpoints · C3 scoped agent toolsets · C4 sequence skills) · scoped/verify/connect-card/sequences all close the real gaps; plan outcome had a quote-style bug (`grep '\''connect'\''` vs source `"connect"`) — fixed in todo. composioScoped mirrors composioFallback; verify uses connectedAccountId + dangerouslySkipVersionCheck per social-publisher pattern. · source=cycle
- 2026-05-29 · agent-first C4 · cycle · declared 27 receiver families into RECEIVERS (51 total) with request schemas mirroring client.ts call sites — build-green proves every payload compile-checks via the C3 conditional generic; rejected the channels migration (generic forwarder, no typed handlers) and the over-specified data:unknown=0 ratchet · rubric=0.925 · source=w1
- 2026-05-29 · billing-software (post-deploy ops) · server chat + composio · After deploying the billing surfaces, three prod/dev issues surfaced and got fixed. (1) IDENTITY_SECRET was set on BOTH one-prod (web) and channels but values couldn't be confirmed to match (secret list shows names only) → owner attestation (the Composio owner-gate) silently failed. Fix: rotated to ONE fresh value on both workers atomically (`wrangler secret put IDENTITY_SECRET --name one-prod` + `--name channels`, same bytes via a 0600 temp file + `<` redirect). Verified by minting a token with the new secret and POSTing prod channels /message → 200 stream, turn started (token accepted). (2) Local chat 503'd because CHANNELS_URL wasn't in .dev.vars (it's in wrangler.toml [vars] but the Astro+CF dev proxy reads runtime string vars from .dev.vars; D1/KV bindings load via platformProxy, which is why cost queries worked but the var didn't). Added CHANNELS_URL + DEV_SLUG=one (the old DEV_SLUG nwvr5oba isn't even a row in owners) + restarted dev. (3) THE BIG ONE — channels chat failed ~80% (4/5 probes) with OpenRouter "This model is not available in your region." Root cause: all personas used `openai/gpt-4o-mini` (OpenAI-only on OpenRouter), and the CF Worker EGRESS IP (not the user's ISP IP — gpt-4o-mini worked fine called directly from the dev machine) geolocates to an OpenAI-blocked region depending on which colo the worker lands in. Not smart-placement (channels has none); it's nearest-colo lottery. Fix: switched all personas + DEFAULT_MODEL to `moonshotai/kimi-k2.6` (latest Kimi, 19 OpenRouter providers, strong tool-calling — verified it emits proper tool_calls) and extended channels' pinOpenRouterProvider shim so non-openai/anthropic vendors inject `provider:{sort:'throughput',allow_fallbacks:true}` → OpenRouter picks the fastest provider and falls past any region-blocked one. Post-deploy probe: 6/6 OK, 0 region errors. LESSON (reinforces the deploy saga): OpenAI-family models are a latent region landmine from CF Workers regardless of placement; prefer multi-provider models + OpenRouter provider-sort so a turn can route around a blocked colo. · channels tsc 0 · source=cycle
- 2026-05-29 · agent-first C5 · cycle · shipped the meta: self-describing surface — meta:catalog/meta:schema as pure SDK projections over RECEIVERS (role-scoped via ownerOnly, z.toJSONSchema for schemas) + meta:recall over world_hypotheses + Backpressure on Outcome; deferred meta:reputation (TypeDB-path data, no-stubs rule); file-link is a COPY so new SDK subpath exports need bun install in web · rubric=0.88 · source=w2
- 2026-05-29 · agent-first C6 · cycle · RECIPES={spine,build,trade,transact} as readonly ReceiverName[] (typo=compile error), runtime-validity tests need no TypeDB so they always run; synced spec RECIPES block · rubric=0.95 · source=w3
- 2026-05-29 · typedb-cloudflare deploy · /do --auto · api+web+channels deployed live; BrainDO active (/brain auth-enforced, /ws 426). Web 500'd on first deploy — '@oneie/sdk' barrel import pulled telemetry's global-scope random into the Worker (init crash); rolled back, fixed with @oneie/sdk/brain subpath, redeployed green. sync(C5) held pending SYNC_SECRET+APP_URL. · source=deploy
- 2026-05-29 · agent-first C7 · cycle · openapi stops lying — generate:openapi injects a ReceiverPayload oneOf (z.toJSONSchema per receiver + x-cost/x-reversible/x-settles) into the hand-spec between markers as ONE flow-JSON line (JSON⊂YAML → byte-stable/idempotent, no YAML dep); MCP signal/ask group by RECIPES + point to meta:catalog; discovered+flagged the legacy generate-types.ts stale path (dead gate, separate fix) · rubric=0.91 · source=w2
- 2026-05-29 · typedb-cloudflare CLOSED · /do --auto · all 6 cycles deployed live + proven: /brain loaded=true, read-after-write (strength=2 from memory), validateEdgeId rejects injection (400), /ws 426, one.ie 200. C5 finished via TypeDB-direct DO reload (no SYNC_SECRET — absent from dev.vars); channels GATEWAY_API_KEY set. paths>0 data-gated (TypeDB has 0 paths). · source=deploy
- 2026-05-29 · agent-first C4–C7 · deploy · shipped live to one-prod (https://one.ie) version 338120ad — generated ReceiverPayload oneOf live in prod openapi.yaml, meta: route deployed (gateway-gated); web deploy bundled in-flight billing/composio (committed agent-first surgically across 3 nested repos) · proof=curl openapi.yaml+403 meta · source=cycle
- 2026-05-30 · receiver-resolvers · cycle · agent lifecycle test found every non-world/non-meta receiver TIMED OUT (routes forwarded to dead NANOCLAW_URL — nanoclaw signal-processor replaced by channels which has no general /signal). Fix: unified in-process dispatchReceiver (world+meta+RESOLVERS) + instant dissolve{no_handler} kills the 10s hang. Also fixed 5 routes 500ing on Astro-v6 locals.runtime.env removal (agent signup was down). Phase-1 reads live in prod (stats:current=25 actors). source=cycle
- 2026-05-30 · receiver-resolvers security · gate · automated review caught 3 in receiver-resolvers.ts: IDOR (groups:members read any tenant) → scoped to ownerSlug via world_groups join; TypeQL injection (weak quote-strip esc) → strict id allowlist ^[A-Za-z0-9:._-]+$; silent-auth on wallet/capability reads → documented as PUBLIC-by-design (marketplace + on-chain pay rail). Verified live: cross-tenant→forbidden, injection→rejected. source=gate
