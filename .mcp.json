{
  "mcpServers": {
    "oneie": {
      "command": "npx",
      "args": [
        "-y",
        "@oneie/mcp@0.10.1"
      ],
      "env": {
        "ONEIE_API_KEY": "${ONEIE_API_KEY:-}",
        "ONEIE_API_URL": "${ONEIE_API_URL:-https://one.ie}"
      }
    }
  }
}
