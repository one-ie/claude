- 2026-05-29 · roles-optimisation C0 · W3 · scope.pattern was `has slug` but `group` is keyed by `gid` (`group:<slug>`) with no slug attr — fixed to gid disjunction + `(group:$g,member:$a) isa membership` join; frontiers filtered to scope=="public" (hypothesis has NO group relation, own-group deferred); descendants-of() added, middleware→recursive CTE · rubric=0.86 · source=w3
- 2026-05-29 · roles-optimisation C4 · W3 · provisionClient SDK sugar over POST /api/groups {parent_gid,invite_email} (endpoint already does all 4 steps); test stubs fetch but must skip the emit() telemetry /api/signal call when capturing · rubric=0.82 · source=w3
- 2026-05-29 · roles-optimisation C9 · W3 · POST /api/groups hardcoded group-type "org"; `type` (agency/client/team) is the org-subtype axis, group-type is the substrate kind — agency+client→org, team→team; team_requires_parent guard added · rubric=0.80 · source=w3
- 2026-05-29 · roles-optimisation C1 · W3 · Astro can't pass fn handlers to a client island; presentational MembersPanel + MembersPanelClient container (the wire site) — downstream cycles add one handler prop, never touch the panel · rubric=0.85 · source=w3
- 2026-05-29 · roles-optimisation C8 · W3 · active_group_id is a per-user Better Auth field, NOT a workspace setting — needs dedicated POST /api/groups/active (membership-gated, fails closed) not settings?scope; pure decideActiveSwitch makes the gate testable · rubric=0.84 · source=w3
- 2026-05-29 · roles-optimisation C2 · W3 · extracted hasAuthorityOver→lib/authority.ts and ROLE_ORDER/canGrantRole→role-check.ts (single source); invites/create.ts now imports them; role change = delete+reinsert (no UPDATE for owned attrs) · rubric=0.86 · source=w3
- 2026-05-29 · roles-optimisation C5 · W3 · createGroup threads parentGid→parent_gid; onAdd reuses invite path (default viewer); zero new server code · rubric=0.82 · source=w3
- 2026-05-29 · roles-optimisation C3 · W3 · DELETE members.ts + pure canRemoveMember last-owner guard (409); revoke composes existing capability-grant · rubric=0.83 · source=w3
- 2026-05-29 · roles-optimisation C7 · W3 · delegate toggle → grant-capability receiver (mint_capability gated), scoped+expiring; active-group caveat real (capabilities fold against activeGroupId — C8 mitigates) · rubric=0.80 · source=w3
- 2026-05-29 · roles-optimisation C6 · W3 · the proof: deterministic sibling-isolation matrix (scopeToGroup + filterByMembership) always runs + skipIf(!GATEWAY) live block — honest no-mocks; plan outcome green · rubric=0.88 · source=w3
- 2026-05-29 · roles-optimisation C10 · W3 · GroupSwitcherTree had exponential double-render (mapped all nodes at every depth) — fixed to roots+recurse-by-parent; wired into GroupSwitcher (category model→hierarchy tree), DivisionSwitcher deleted, act-as preserved · rubric=0.81 · source=w3
- 2026-05-29 · roles-optimisation C10 · REVERT · category→tree switcher buried clients under agencies (UX regression user caught); reverted to categories, kept C8 navigate re-scope + tree double-render fix + DivisionSwitcher deletion · rubric=n/a · source=user-feedback
- 2026-05-29 · roles-optimisation C11 · W3 · top×bottom coupling: AuthButton act-as scoped to active group via ?group=<Astro.locals.slug>; re-mounts on switch (full nav) so impersonation list always = current group's members · rubric=0.85 · source=w3
- 2026-05-29 · roles-optimisation C12 · W3 · GroupSwitcher rewritten as parent→child drill (membership-scoped tree, per-level search): body-click switches (C8 re-scope), chevron drills; act-as removed from top (now bottom). Scales 3→300 children · rubric=0.84 · source=w3
- 2026-05-29 · roles-optimisation · FIX (ran the app, stopped guessing) · BOTTOM impersonation empty because humanTargets filtered model==="human" but actor-type data is "person" → accept person+human. TOP "only agencies" = (a) /api/groups/tree extraRows query shape the gateway didn't answer (clients dropped) → simplified to fetch-all-and-filter + taught the dev stub the shape; (b) drill rooted at group:one burying orgs → isRoot treats universal root group:one as transparent. Verified in browser: bottom lists people, top shows agencies→drill→clients (oo→boq,globex). · rubric=0.88 · source=user-feedback+browser
- 2026-05-29 · billing-software C1–C6 · BUILD (engine + pricing surface) · Shipped the spec core "one credit, three verbs, one ledger, one document": 6 idempotent migrations 0076–0081 (sqlite-validated; avoided expires_at re-add drift — 0016 already had it), grant.ts (proration+FIFO), burn.ts (tiered+meters), gate.ts (entitlements) as pure cores + thin D1 wrappers, invoice.ts (draft→compute→finalize+coupons+credit FIFO), and C6 pricing (public.ts/subscribe.ts/PlanCards/pricing.astro — getPublicPlans shared so "Pro" renders SSR-side, not just in the client island). 31 vitest passing, tsc delta=0. · rubric=0.84 · source=w3
